import SideBar from "./SideBar";

export default function Notifications() {
    return (
        <>
            <SideBar />
            <div>
                <h2>Notifications Page</h2>
                <p>This is where users can view their notifications.</p>
            </div>
        </>
  );
}